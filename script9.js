Boo Who
function booWho(bool) {
  // What is the new fad diet for ghost developers? The Boolean.
  // check if the type of bool is a boolean
  if(typeof bool === 'boolean'){
    // if it is, return true
    return true;
  }
  // if is not, return false
  else{
    return false;
  }


}

booWho(null);
